# Copy to frontend/.env for local dev
VITE_API_URL=http://localhost:8000

# Production Docker (CI): leave GitHub secret VITE_API_URL empty — Nginx proxies /api to the API container.
# Set VITE_API_URL only if the frontend calls a separate API domain.
