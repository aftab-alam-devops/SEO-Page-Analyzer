# Default values for local monitoring stack.

prometheus:
  image: prom/prometheus:v2.51.0
  service:
    type: LoadBalancer
    port: 9090

grafana:
  image: grafana/grafana:10.4.0
  service:
    type: LoadBalancer
    port: 3000
  adminUser: "admin"
  adminPassword: "admin_new_password"
